JPA : JAVA 언어를 DB가 이해할 수 있게 하며 데이터 관리를 편리하게 하는 기능들도 제공

→ 핵심도구 : Entity, Repository
Entity : Java 객체를 DB가 이해할 수 있게 잘 규격화된 데이터
Repository : 규격화된 Entity는 repository를 통해 DB에 전달, 처리

DTO → Entity → Repository → DB