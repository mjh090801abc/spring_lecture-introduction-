Lombok : 코드를 간소화 시켜주는 라이브러리
리펙토링 : 코드의 구조 또는 성능을 개선하는 작업

@AllargsConstructor : 클래스에 생성된 모든 필드를 매개변수로 받는 생성자를 자동으로 생성
@ToStirng : tostring 메소드 자동 생성
@Slf4j : 로깅을 위한 어노테이션

log.info(); == System.out.println();
→ 스프링에서는 log.info();를 활용하여서 서버에서 일어나는 일들을 기록할 수 있음.